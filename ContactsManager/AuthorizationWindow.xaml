<Window x:Class="ContactsManager.AuthorizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContactsManager"
        xmlns:ViewModel="clr-namespace:ViewModel;assembly=ViewModel"
        mc:Ignorable="d"
        Title="AuthorizationWindow" Height="330" Width="458">
    <Window.DataContext>
        <ViewModel:AuthorizationVM/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Image Grid.Row="0" HorizontalAlignment="Center" Height="100" Width="84" Source="/icons/google_contact_main.png" Margin="0,40,0,10"/>

        <Label Grid.Row="1" Content="Google Contact Manager" HorizontalAlignment="Center" FontSize="22" Margin="0,10,0,10"/>

        <Button x:Name="LoginButton" Grid.Row="2" Content="Войти" Command="{Binding LoginCommand}"
            Width="120" Height="50" FontSize="16"
            VerticalAlignment="Top"
            Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,20,0,0" >
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border CornerRadius="20">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#4facfe" Offset="0.0"/>
                                <GradientStop Color="#00f2fe" Offset="1.0"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>

</Window>
