<UserControl x:Class="View.Controls.ValuePicker.GoogleDatePickerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel x:Name="RootPanel">
        <StackPanel.Resources>
            <!-- Валидатор -->
            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="6" />
                <Setter Property="Padding" Value="4" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="BorderBrush" Value="#FFCCCCCC" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Background" Value="White" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Style.Triggers>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Setter Property="BorderBrush" Value="#FF007ACC" />
                        <!--<Setter Property="BorderThickness" Value="2" />-->
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.6" />
                    </Trigger>
                    <Trigger Property="Validation.HasError" Value="True">
                        <Setter Property="BorderBrush" Value="#E53935" />
                        <Setter Property="BorderThickness" Value="1" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="TextBlock">
                <Style.Triggers>
                    <Trigger Property="Validation.HasError" Value="True">
                        <Setter Property="Visibility" Value="Visible" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </StackPanel.Resources>

        <StackPanel Orientation="Horizontal" Margin="10" VerticalAlignment="Center">
            <!-- День -->
            <StackPanel Margin="0,0,15,0">
                <Label Content="День" />
                <TextBox Width="50" MaxLength="2"
                         CommandManager.PreviewExecuted="NumberTextBox_PreviewExecuted"
                         PreviewTextInput="NumberTextBox_PreviewTextInput"
                     Text="{Binding Day, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" />
            </StackPanel>

            <!-- Месяц -->
            <StackPanel Margin="0,0,15,0">
                <Label Content="Месяц" />
                <ComboBox Width="150"
                      ItemsSource="{Binding MonthsNamed}"
                      SelectedItem="{Binding SelectedMonth, ValidatesOnDataErrors=True}"
                      DisplayMemberPath="Item2" />
            </StackPanel>

            <!-- Год -->
            <StackPanel>
                <Label Content="Год" />
                <TextBox Width="70" MaxLength="4"
                        CommandManager.PreviewExecuted="YearTextBox_PreviewExecuted"
                        PreviewTextInput="YearTextBox_PreviewTextInput"
                     Text="{Binding Year, ValidatesOnDataErrors=True}" />
            </StackPanel>
        </StackPanel>
        <TextBlock  Foreground="Red"
                    FontSize="12"
                    Margin="10,0,0,0"
                    Visibility="Collapsed"
                    Text="Дата некоректна" />
    </StackPanel>
</UserControl>